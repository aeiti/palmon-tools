// Evolutions of base palmon species. Keyed by base species key.
//
// Stored for future use — not surfaced in the UI yet. Per Glacewing →
// Cryovern, the evolved form is its own named palmon with the base species'
// four skills plus an additional evolution skill (modeled separately when
// that data lands).

export const PALMON_EVOLUTIONS = {
  glacewing: { name: 'Cryovern' },
};
